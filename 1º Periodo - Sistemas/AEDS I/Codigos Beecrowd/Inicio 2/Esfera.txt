#include <stdio.h>
#include <math.h>
 
int main() {
    
    double VOLUME,pi,R;
    scanf("%lf",&R);
    pi = 3.14159;
    VOLUME = (4/3.0)*pi*pow(R,3);
    printf("VOLUME = %.3lf\n",VOLUME);
    return 0;
}