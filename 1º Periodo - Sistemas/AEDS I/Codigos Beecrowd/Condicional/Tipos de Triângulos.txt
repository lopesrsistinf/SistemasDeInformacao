#include <stdio.h>
 
int main()
{
    double valores[3],A,B,C;
    scanf("%lf %lf %lf",&valores[0],&valores[1],&valores[2]);
    if (valores[0]>valores[1] && valores[0]>valores[2])
    {
        A = valores[0];
    }
    else if (valores[1] > valores[0] && valores[1] > valores[2])
    {
        A = valores[1];
    }
    else if (valores[2] > valores[0] && valores[2] > valores[1])
    {
        A = valores[2];
    }
    else if (valores[2] == valores[0] || valores[2] == valores[1])
    {
        A = valores[2];
        B = valores[2];
    }
    else if (valores[1] == valores[0] || valores[1] == valores[2])
    {
        A = valores[1];
        B = valores[1];
    }
    else if (valores[0] == valores[2] || valores[0] == valores[1])
    {
        A = valores[0];
        B = valores[0];
    }
    if (A == valores[0] && valores[1]>valores[2])
    {
        B = valores[1];
        C = valores[2];
    }
    else if (A == valores[0] && valores[2]>valores[1])
    {
        B = valores[2];
        C = valores[1];
    }
    else if (A == valores[0] && valores[2]==valores[1])
    {
        B = valores[2];
        C = valores[2];
    }
    if (A == valores[1] && valores[0]>valores[2])
    {
        B = valores[0];
        C = valores[2];
    }
    else if (A == valores[1] && valores[2]>valores[0])
    {
        B = valores[2];
        C = valores[0];
    }
    else if (A == valores[1] && valores[2]==valores[0])
    {
        B = valores[2];
        C = valores[2];
    }
    if (A == valores[2] && valores[0]>valores[1])
    {
        B = valores[0];
        C = valores[1];
    }
    else if (A == valores[2] && valores[1] > valores[0])
    {
        B = valores[1];
        C = valores[0];
    }
    else if (A == valores[2] && valores[1] == valores[0]){
        B = valores[1];
        C = valores[1];
    }

    //printf("%.2lf %.2lf %.2lf\n\n",A,B,C);

    if (A>0 && B>0 && C>0){
        if (A >= B+C){
            printf("NAO FORMA TRIANGULO\n");
            return 0;
        }
        if ((A*A) == ((B*B)+(C*C))){
            printf("TRIANGULO RETANGULO\n");
        }
        if ((A*A) > ((B*B)+(C*C))){
            printf("TRIANGULO OBTUSANGULO\n");
        }
        if ((A*A) < ((B*B)+(C*C))){
            printf("TRIANGULO ACUTANGULO\n");
        }
        if (((A==B) && (A!=C)) || ((A==C) && (A!=B)) || ((B==C) && (B!=A))){
            printf("TRIANGULO ISOSCELES\n");
        }
        if ((A == B) && (B==C)){
            printf("TRIANGULO EQUILATERO\n");
        }
    }

    return 0;
}