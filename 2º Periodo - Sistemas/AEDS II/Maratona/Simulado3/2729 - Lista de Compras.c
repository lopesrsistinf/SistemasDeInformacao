#include <stdio.h>
#include <stdlib.h>

int main(){
    int N;
    char lista[21005],nova[21005];
    scanf("%d",&N);
    while(N--){
        while(scanf(" %s")) != NULL){
            scanf(" %s",lista);

        }    
    }
}