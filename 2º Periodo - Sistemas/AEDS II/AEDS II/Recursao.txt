#include <stdio.h>
#include <stdlib.h>

/*
int maiorVetor (int V[],int i,int f){
    int m;
    if (i == f) return V[i];
    m = maiorVetor(V,i+1,f);
    if (m > V[i]) return m;
    return V[i];
}

int main()
{
    int V[] = {5,0,2,1,4,5,6,7,4,2,1,6,85,23,42,68,86};
    int m;
    m = maiorVetor(V,0,16);
    printf("%d\n",m);
    return 0;
}*/

/*
int fatorial (int n){
    int p;
    if(n==0) return 1;
    else{
        p = n*fatorial(n-1);
        printf("%d * %d = %d\n",n,n-1,p);
        return p;
    }
}

int main(){
    int fatorar,fatorado;
    scanf("%d",&fatorar);
    fatorado = fatorial(fatorar);
    printf("Valor Fatorado = %d\n",fatorado);
    return 0;
}

int fibo(int n){
    if(n == 0|| n == 1)
        return n
    else
    return fibo(n-1) + fibo(n-2);
}
*/

